---
interface Props {
  text: string;
  class?: string;
}
const { text, class: className = '' } = Astro.props;
---
<span class={`font-barlow text-[0.65rem] uppercase tracking-[0.15em] text-accent-blue ${className}`}>
  {text}
</span>
