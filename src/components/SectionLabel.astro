---
interface Props {
  text: string;
  class?: string;
}
const { text, class: className = '' } = Astro.props;
---
<span class={`font-barlow text-[1rem] font-semibold uppercase tracking-[0.15em] text-accent-blue ${className}`}>
  {text}
</span>
